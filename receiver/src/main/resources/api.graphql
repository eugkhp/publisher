schema {
    query: Query
    mutation: Mutation
}

type Mutation {
    subscribe(address: String!): Boolean!
    unsubscribe(address: String!): Boolean!
    publish(data: String!): Boolean!
}

type Query {
    history(from: Int!, n: Int!): [String!]!
}

